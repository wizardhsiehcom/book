# 可讀性回修：從實驗摘要改成帶做的書

日期：2026-09-22。基準 commit：`8c660ac`。需求來自本次對話：使用者指出「書籍難以看懂」，確認開始修正。此工作不延續 push，不修改其他書籍與首頁改動。

## 問題與驗收

原版已驗工具可執行，但沒有充分交代陌生專案的場景、改動位置與結果到下一步的推理。回修不以字數或圖數替代教學品質。

- 新增接手場景：分清 Jobs 表、JobRow 值、計算結果與持久化寫回。
- 首讀路線固定為開場 → 純 C++ 準備 → 01–04 → SQL 準備 → 05–17，不從第 03 章跳回第 01 章。
- 02–03 使用可編譯的 `first_job.cpp`，讓讀者親手從鍵盤入口改成固定輸入，再加開關；不是只執行成品 CLI。
- 第一次影響操作的術語就在該處解釋；每次提供修改位置、基準、預測、觀察、差異的下一個動作。
- SQL 案例保持既有隔離實作，正文按實際函式與輸出拆開讀；明說腳本自動連跑，不冒充手動分步。
- 區分實際原碼、要替換的區塊與不可直接編譯的示意；不新增虛構選項。
- 保留每章一個遷移判斷題、已驗／未驗界線。診斷用固定值不當作正式資料正確的證明。
- 不改 SQL 行為、不存私有連線資料、不建立新通知或並行去重系統。

## 驗證安排

1. 新測試從章節的實際 C++ code fence 取出替換片段，編譯互動、固定 10／9、test_mode on／off。驗證未重建不生效、同核心呼叫與 NOT EXECUTED。
2. 公開 `build.cmd` 完整建置與 `first` 分支分開驗，保留既有 16 個單元測試。
3. MkDocs strict、本地連結與出版邊界、全部 Mermaid 的 Chrome SVG 渲染。
4. 分開做文件規範與讀者需求檢查；repo 未設定 engineering skill 的 issue tracker，不為這次編書新增外部 issue 或專案治理設定。審查使用本檔與 `CLAUDE.md`。

本次可證明程式片段能執行、敘述與程式一致，不能替代目標讀者實際閱讀回饋。使用者是否能順利跟做仍須閱讀驗收。

## 交接續修：2026-09-22

接續既有未提交變更，不重建 Docker 環境、不修改 SQL 案例行為，也不處理其他書籍。

| 範圍 | 本輪完成 |
|---|---|
| 05 | 先認識 env／dbc／statement，再逐行讀 diag；區分原始 SQL rc 與 diag_rc，指出 PASS 只斷言 SQL_ERROR |
| 06–08 | 對照 SELECT 欄位、小 buffer、兩層結果迴圈與 bind storage；先預測再執行，逐段連回真實輸出 |
| 09 | 明列待加入專案的函式、允許／拒絕測試與整合步驟；沒有新 CLI、archive 表或已驗結果 |
| 10 | 三句 UPDATE、1／0／1、execute 的 SQL_NO_DATA、交易內後置條件與 rollback 後獨立讀回分開解釋 |
| 12–13 | 拆短鎖等待片段；校正清理由 C++ connection 管理、清理 NULL 未另讀回、timing 沒有時間／score 斷言；補第三次 baseline |
| 14–17 | 補工具條件對照、重試兩條路徑、檔案差異與候選產物驗收；維持純設計與實測界線 |
| 全書銜接 | 核對前段場景與入口、命令工作目錄、圖文名稱、示意／原碼標記、PASS 範圍；第 01 章改明 exe 由讀者自行建置 |

審查分開核對規範與需求：規範以 `CLAUDE.md` 的繁體中文、相對連結、Mermaid 與出版範圍為準；需求以本檔的操作順序、小片段、預測與解讀為準。這是本輪文字與原碼核對，不宣稱重新完成來源研究或目標讀者驗收。

本輪已重新以公開腳本編譯三個 target，跑過全部 24 個測試；strict build、出版結構／連結檢查通過，Chrome 確認全部 10 圖為 `rendered=1 error=0`。SQL 實測數字仍來自初版紀錄，未重跑 DB、未增加故障模型。沒有新的系統行為假說被實驗證偽，因此不新增 `docs/misses.jsonl` 紀錄。未 commit／push，保留其他 dirty worktree。
