# Sync Agent

Use this skill when Codex or Antigravity needs to run the `/sync-agent`
workflow for this repository.

The canonical workflow definition is `.claude/commands/sync-agent.md`.
Read that file before acting, then follow its variables, instructions, workflow,
and report format.
