# MkDocs Create

Use this skill when Codex or Antigravity needs to run the `/mkdocs-create`
workflow for this repository.

The canonical workflow definition is `.claude/commands/mkdocs-create.md`.
Read that file before acting, then follow its variables, instructions, workflow,
and report format.
