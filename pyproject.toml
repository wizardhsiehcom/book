[project]
name = "newbook"
version = "0.1.0"
description = "MkDocs migration preview for cowos"
requires-python = ">=3.10"
dependencies = [
  "mkdocs>=1.6",
  "mkdocs-material>=9.5",
  "pymdown-extensions>=10.0",
]
